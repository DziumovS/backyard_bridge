[pytest]
addopts =
    --strict-markers
    --cov=main
    --cov=src
    --cov-config=.coveragerc
    --cov-report=term-missing
    --cov-report=html
    --cov-fail-under=99
testpaths = tests
markers =
    e2e: tests that exercise the running ASGI application end to end
